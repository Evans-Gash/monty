#include "monty.h"

/**
 * f_stack - Sets the list format to stack (LIFO - Last-In-First-Out)
 * @head: Pointer to the head of the stack
 * @counter: Line number
 * Return: void
 */
void f_stack(stack_t **head, unsigned int counter)
{
        (void)head;
        (void)counter;
        bus.lifi = 0;
}
